/**
 * EQUALITY AND RELATIONAL OPERATORS
 * == equal
 * != not equal
 * > greater than
 * < less than
 * >= greater than or equal to
 * <= less than or equal to
 */

void main() {
  assert(2 == 2);
  assert(2 != 3);
  assert(3 > 2);
  assert(2 < 3);
  assert(3 >= 3);
  assert(2 <= 3);
}
